/**
 * Monitor de handoff desativado — não observa nem abre arquivos .cursor/.
 * Mantido apenas para compatibilidade com npm run cursor:watch-console.
 */
console.info('[contabilfacil-console] Monitor de handoff desativado pelo usuário.');
